#include "main.h"

/**
* mul - multiple integers or checks for the two integers
* @a: a - Variable
* @b: b - variable
* code by ck
* Return: Always 0.
*/

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
